<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <!--
    <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorPrimaryLight">@color/colorPrimaryLight</item>
        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="colorSecondaryDark">@color/colorSecondaryDark</item>
        <item name="colorSecondaryLight">@color/colorSecondaryLight</item>
    -->

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- CardView style -->
    <style name="CardViewItemTheme" parent="AppTheme">
        <item name="android:clickable">true</item>
        <item name="android:layout_margin">8dp</item>
    </style>

    <!-- History item style -->
    <style name="HistoryItemImageTheme" parent="AppTheme">
        <item name="android:layout_width">@dimen/history_item_image_width</item>
        <item name="android:layout_height">@dimen/history_item_image_height</item>
        <item name="android:layout_margin">6dp</item>
    </style>

    <style name="HistoryItemTextTheme" parent="AppTheme">
        <item name="android:textColor">@color/colorHistoryItemLabel</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">italic</item>
        <item name="android:layout_marginLeft">6dp</item>
        <item name="android:layout_marginRight">6dp</item>
    </style>

</resources>
